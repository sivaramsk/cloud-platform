grafana:
  enabled: true
  grafana.ini:
    smtp:
       enabled: true
       host: smtp.gmail.com:587
       user: "demo_emulya@intainft.com"
       password: "welcome@12"
       skip_verify: true

prometheus:
  enabled: true
  prometheusSpec:
    storageSpec: 
      volumeClaimTemplate:
        spec:
          storageClassName: azurefile
          resources:
            requests:
              storage: 10Gi
